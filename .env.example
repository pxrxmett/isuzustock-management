# ===== Application =====
NODE_ENV=development
PORT=3000

# ===== Database Configuration =====
# Option 1: Use DATABASE_URL (for Railway, Heroku, etc.)
# DATABASE_URL=mysql://username:password@host:port/database

# Option 2: Use individual variables (for local development)
DB_HOST=localhost
DB_PORT=3306
DB_USERNAME=stockuser
DB_PASSWORD=your_secure_password_here
DB_DATABASE=stock_management

# ===== JWT Secret =====
# IMPORTANT: Generate a strong random secret for production
# You can use: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your_jwt_secret_key_change_this_in_production

# ===== LINE Credentials =====
# Get these from LINE Developers Console: https://developers.line.biz/
LINE_CHANNEL_ID=your_line_channel_id_here
LINE_CHANNEL_SECRET=your_line_channel_secret_here
LINE_LIFF_ID=your_line_liff_id_here

# ===== CORS Configuration =====
# Frontend URL for CORS (optional, comma-separated for multiple origins)
FRONTEND_URL=http://localhost:8080
