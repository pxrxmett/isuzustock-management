# =============================================================================
# Railway Production Environment Variables
# คัดลอกไฟล์นี้ไปวางใน Railway Dashboard > Variables
# =============================================================================

# Environment
NODE_ENV=production

# =============================================================================
# Database Configuration
# ⚠️ คัดลอกค่าจริงจาก MySQL Service Variables มาวางตรงนี้
# =============================================================================
# ไปที่ MySQL Service > Variables แล้วคัดลอกค่าเหล่านี้มา:

DB_HOST=mysql.railway.internal
DB_PORT=3306
DB_USERNAME=root
DB_PASSWORD=COPY_FROM_MYSQL_SERVICE_MYSQLPASSWORD
DB_DATABASE=railway

# =============================================================================
# JWT Configuration
# ⚠️ สร้าง JWT Secret ใหม่สำหรับ Production (ใช้ openssl rand -base64 64)
# =============================================================================
JWT_SECRET=GENERATE_STRONG_SECRET_HERE_MIN_64_CHARACTERS
JWT_EXPIRES_IN=7d
JWT_REFRESH_SECRET=GENERATE_ANOTHER_STRONG_SECRET_FOR_REFRESH_TOKEN
JWT_REFRESH_EXPIRES_IN=30d

# =============================================================================
# LINE Integration
# ⚠️ ใช้ค่าจริงจาก LINE Developers Console
# =============================================================================
LINE_CHANNEL_ID=2006746784
LINE_CHANNEL_SECRET=e673f3def0fecc4eeb43aad4460381fa
LINE_CHANNEL_ACCESS_TOKEN=GET_LONG_LIVED_TOKEN_FROM_LINE_DEVELOPERS_CONSOLE
LINE_LIFF_ID=2006746784-e1y9NRqn

# =============================================================================
# Frontend URLs
# ⚠️ ใส่ URL ของ Frontend Apps ที่ deploy บน Vercel
# =============================================================================
FRONTEND_URL=https://testdrive-liff-app.vercel.app
ADMIN_URL=https://admin-dashboard.vercel.app

# =============================================================================
# 📋 วิธีใช้:
# =============================================================================
# 1. คัดลอกทุกอย่างจากไฟล์นี้
# 2. ไปที่ Railway Dashboard > Backend Service > Variables
# 3. กดปุ่ม "Raw Editor" (มุมขวาบน)
# 4. วางทั้งหมดลงไป
# 5. แก้ไขค่าที่มี COPY_FROM_... และ GENERATE_...
# 6. กด "Deploy" (Railway จะ redeploy อัตโนมัติ)
#
# 🔍 วิธีหาค่าจาก MySQL Service:
# - ไปที่ MySQL Service > Variables
# - คัดลอกค่าจาก:
#   * MYSQLHOST → ใส่ใน DB_HOST
#   * MYSQLPORT → ใส่ใน DB_PORT
#   * MYSQLUSER → ใส่ใน DB_USERNAME
#   * MYSQLPASSWORD → ใส่ใน DB_PASSWORD
#   * MYSQLDATABASE → ใส่ใน DB_DATABASE
#
# 🔐 วิธีสร้าง JWT Secret:
# - รันคำสั่ง: openssl rand -base64 64
# - หรือใช้เว็บ: https://generate-secret.vercel.app/64
# =============================================================================
